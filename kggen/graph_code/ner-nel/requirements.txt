# Requirements for wisefood/foodscholar/graph_code/ner-nel
#
# Derived from the imports of the four files in this folder
# (nel_ner_evaluation.ipynb, ner_benchmark_cross_dataset.ipynb,
#  run_ner_nel_corpus_gliner2_sapbert.py, run_scifoodner_inference.py).
# Versions are the exact ones installed in the `python3.12_venv` environment
# (Python 3.12.3), pinned as they resolve there.
#
# Not listed on purpose:
#   - `src` (imported as `from src.nel import LexicalNELLinker, HNSWNELLinker`) is a
#     local, repo-provided package, not a PyPI distribution.
#   - stdlib modules (argparse, csv, gc, inspect, json, logging, os, pathlib,
#     subprocess, sys, time, warnings).
#
# Module -> distribution names that differ:
#   IPython          -> ipython
#   huggingface_hub  -> huggingface-hub
#   simpletransformers -> simpletransformers  (SciFoodNER; the script's docstring
#                        runs it with the dedicated `scifoodner` conda env, e.g.
#                        /mnt/data/makis/conda_envs/scifoodner/bin/python)

gliner==0.2.26
gliner2==1.3.1
huggingface-hub==0.36.2
ipython==8.22.2
matplotlib==3.10.6
numpy==2.5.1
pandas==3.0.5
requests==2.34.2
seaborn==0.13.2
simpletransformers==0.70.5
spacy==3.8.7
torch==2.7.0
transformers==4.57.6
